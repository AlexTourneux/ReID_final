# Minimal deps. Base packages (torch, torchvision, numpy, pandas, pillow,
# pyarrow, tqdm, etc.) come preinstalled on Colab and Kaggle — don't pin
# their versions here or we'll fight the cloud env.
#
# For a reproducible local setup, install torch/torchvision via the
# official index matching your CUDA/MPS, then `pip install -r requirements.txt`.

PyYAML>=6.0
timm>=0.9.16
transformers>=4.40.0
gdown>=5.2.0
